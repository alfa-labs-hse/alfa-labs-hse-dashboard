/* Alfa Labs HSE shared access + dashboard recovery */
(function(){'use strict';if(window.__ALFA_AUTH__)return;window.__ALFA_AUTH__=true;
const CONFIG={allowedDomain:'@alfalabs.com',users:[{email:'hossam.elsharabasy@alfalabs.com',role:'admin'},{email:'mohamed.nasr@alfascan.com',role:'viewer',expiresAt:1789774860000}],sessionKey:'alfaLabsAuthSession'};
const getSession=()=>{try{return JSON.parse(sessionStorage.getItem(CONFIG.sessionKey)||'null')}catch(e){return null}};
const clearSession=()=>sessionStorage.removeItem(CONFIG.sessionKey);
function isAllowed(email){const e=String(email||'').trim().toLowerCase(),u=CONFIG.users.find(x=>x.email.toLowerCase()===e);return !!u&&(!u.expiresAt||Date.now()<u.expiresAt)&&(e.endsWith(CONFIG.allowedDomain)||e==='mohamed.nasr@alfascan.com')}
function setSession(email,role){const u=CONFIG.users.find(x=>x.email.toLowerCase()===String(email||'').trim().toLowerCase());sessionStorage.setItem(CONFIG.sessionKey,JSON.stringify({email,role,loginAt:Date.now(),expiresAt:u?.expiresAt||null}))}
function guard(){if((location.pathname.split('/').pop()||'index.html').toLowerCase()==='login.html')return true;const s=getSession();if(!s||!isAllowed(s.email)||(s.expiresAt&&Date.now()>=s.expiresAt)){clearSession();location.replace('./login.html?return='+encodeURIComponent(location.href));return false}document.documentElement.dataset.alfaRole=s.role;document.body.dataset.alfaRole=s.role;return true}
function toast(msg){let t=document.getElementById('alfa-auth-toast');if(!t){t=document.createElement('div');t.id='alfa-auth-toast';document.body.appendChild(t)}t.textContent=msg;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),2200)}
function viewerLock(){const s=getSession();if(!s||s.role==='admin')return;document.addEventListener('click',e=>{const t=e.target.closest('button,a,[role="button"]');if(t&&/(export|download|print|copy|save report|get a report|تحميل|تنزيل|طباعة|نسخ|تقرير)/i.test(t.innerText||t.title||'')){e.preventDefault();e.stopPropagation();toast('View-only access — this action is restricted.')}},true);document.addEventListener('contextmenu',e=>e.preventDefault());document.addEventListener('copy',e=>e.preventDefault());document.addEventListener('cut',e=>e.preventDefault());document.addEventListener('keydown',e=>{if((e.ctrlKey||e.metaKey)&&['c','x','s','p','u'].includes(e.key.toLowerCase()))e.preventDefault()},true)}
function addBadge(){const s=getSession();if(!s||document.getElementById('alfa-user-badge'))return;const host=document.querySelector('.unified-meta,.header-actions,.user')||document.body,el=document.createElement('div');el.id='alfa-user-badge';el.style.cssText='display:flex;align-items:center;gap:8px;direction:ltr';el.innerHTML='<b>'+(s.role==='admin'?'ADMIN':'VIEW ONLY')+'</b><span>'+s.email.split('@')[0]+'</span><button id="alfa-logout" type="button">Logout</button>';host.appendChild(el);el.querySelector('button').onclick=()=>{clearSession();location.replace('./login.html')}}
function init(){if(!guard())return;viewerLock();addBadge()}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();window.AlfaHSEAuth={CONFIG,getSession,setSession,clearSession,isAllowed,roleFor:e=>CONFIG.users.find(x=>x.email.toLowerCase()===String(e||'').trim().toLowerCase())?.role||null,guard,toast};})();
/* Home CAPA recovery: resolve JSON relative to this script and retry after page load. */
(function(){const p=location.pathname.toLowerCase();if(!(p==='/'||p.endsWith('/index.html')||p.endsWith('/alfa-labs-hse-dashboard/')))return;const norm=v=>String(v??'').normalize('NFKC').replace(/[\u064B-\u065F\u0670\u0640]/g,'').toLowerCase();const risk=v=>{let s=norm(v);return s.includes('critical')||s.includes('حرج')?'Critical':s.includes('high')||s.includes('عالي')?'High':s.includes('medium')||s.includes('متوسط')?'Medium':s.includes('low')||s.includes('منخفض')?'Low':''};const status=v=>{let s=norm(v);return s.includes('progress')||s.includes('تنفيذ')?'In Progress':s.includes('open')||s.includes('مفتوح')?'Open':s.includes('closed')||s.includes('مغلق')?'Closed':''};const fmt=n=>Number(n||0).toLocaleString('en-US');const put=(id,v)=>{const e=document.getElementById(id);if(e)e.textContent=fmt(v)};async function run(){try{const tag=[...document.scripts].find(s=>s.src.includes('auth.js'));const u=new URL('./capa-data.json',tag?.src||document.baseURI);u.searchParams.set('v',Date.now());const r=await fetch(u,{cache:'no-store'});if(!r.ok)throw Error('HTTP '+r.status);const d=await r.json();if(!Array.isArray(d))throw Error('Unexpected CAPA JSON format');const c={Critical:0,High:0,Medium:0,Low:0,Open:0,'In Progress':0,Closed:0};d.forEach(x=>{const a=risk(x.risk),b=status(x.status);if(a)c[a]++;if(b)c[b]++});[['totalValue',d.length],['criticalValue',c.Critical],['highValue',c.High],['openValue',c.Open],['progressValue',c['In Progress']],['closedValue',c.Closed],['donutTotal',d.length],['legendOpen',c.Open],['legendProgress',c['In Progress']],['legendClosed',c.Closed],['actionCritical',c.Critical],['actionHigh',c.High],['actionOpen',c.Open]].forEach(([id,v])=>put(id,v));const ch=document.getElementById('riskChart');if(ch){const m=Math.max(c.Critical,c.High,c.Medium,c.Low,1);ch.innerHTML=['Critical','High','Medium','Low'].map(k=>'<div class="bar-wrap"><div class="bar-value">'+fmt(c[k])+'</div><div class="bar '+k.toLowerCase()+'" style="height:'+Math.max(4,c[k]/m*88)+'%"></div><div class="bar-label">'+k+'</div></div>').join('')}const t=d.length,a=t?c.Open/t*100:0,b=t?c['In Progress']/t*100:0,don=document.getElementById('statusDonut');if(don)don.style.background='conic-gradient(#315d91 0 '+a+'%,#d97722 '+a+'% '+(a+b)+'%,#23834b '+(a+b)+'% 100%)';const sr=document.getElementById('statusRate');if(sr)sr.textContent=t?Math.round(c.Closed/t*100)+'% closed':'—';const ds=document.getElementById('dataStatus');if(ds)ds.textContent='CAPA live data connected • '+fmt(t)+' records';}catch(e){console.error('[Home CAPA]',e);const ds=document.getElementById('dataStatus');if(ds)ds.textContent='CAPA data load failed: '+e.message}}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',run);else run();window.addEventListener('load',()=>setTimeout(run,500));setTimeout(run,1400)})();
/* HSE Teams display merge for Raymond/ريمون spelling variants (preserves row totals). */
(function(){if(!location.pathname.toLowerCase().endsWith('/hse-teams.html'))return;const canon=s=>{const v=String(s||'').normalize('NFKC').replace(/[\u064B-\u065F\u0670\u0640]/g,'').toLowerCase().replace(/[^\p{L}\p{N}]/gu,'');if(v.includes('ريمون')||v.includes('raymond')||v.includes('raimond'))return'__raymond__';return v};let busy=false;function merge(){if(busy)return;const body=document.getElementById('teamRows');if(!body)return;const trs=[...body.querySelectorAll('tr')].filter(tr=>tr.cells.length>2);if(!trs.length)return;busy=true;try{const groups=new Map();trs.forEach(tr=>{const k=canon(tr.cells[1]?.textContent);if(!k)return;if(!groups.has(k)){groups.set(k,tr);return}const keep=groups.get(k);for(let i=2;i<Math.min(keep.cells.length,tr.cells.length);i++){const a=Number(keep.cells[i].textContent.replace(/[^\d.-]/g,''))||0,b=Number(tr.cells[i].textContent.replace(/[^\d.-]/g,''))||0;const target=keep.cells[i].querySelector('.pill')||keep.cells[i];target.textContent=(a+b).toLocaleString('en-US')}tr.remove()});const sel=document.getElementById('memberSelect');if(sel){const seen=new Set();[...sel.options].slice(1).forEach(o=>{const k=canon(o.textContent);if(seen.has(k))o.remove();else seen.add(k)})}const remaining=[...body.querySelectorAll('tr')].filter(tr=>tr.cells.length>2);const sums=remaining.reduce((a,tr)=>{for(let i=2;i<Math.min(8,tr.cells.length);i++)a[i]=(a[i]||0)+(Number(tr.cells[i].textContent.replace(/[^\d.-]/g,''))||0);return a},{});const ids=['trainedCount','capaCount','openCount','closedCount'];[3,4,5,7].forEach((col,j)=>{const el=document.getElementById(ids[j]);if(el)el.textContent=(sums[col]||0).toLocaleString('en-US')});const mc=document.getElementById('memberCount');if(mc)mc.textContent=remaining.length.toLocaleString('en-US');const tc=document.getElementById('tableCount');if(tc)tc.textContent=remaining.length+' team members';}finally{busy=false}}const start=()=>{merge();const body=document.getElementById('teamRows');if(body)new MutationObserver(()=>merge()).observe(body,{childList:true,subtree:true})};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start);else start();})();
/* Homepage stability patch: remove the leaked dashboard source text (not script elements). */
(function(){if(!/(^|\/)index\.html$/.test(location.pathname)&&location.pathname!=='/')return;function cleanLeakedSource(){const walker=document.createTreeWalker(document.body,NodeFilter.SHOW_TEXT);const remove=[];let n;while((n=walker.nextNode())){const t=n.nodeValue||'';if(t.includes('function norm(v){return String(v??\'\').trim().toLowerCase()}')&&t.includes('loadCapa();')&&t.length>500)remove.push(n)}remove.forEach(n=>n.parentNode&&n.parentNode.removeChild(n))}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',cleanLeakedSource);else cleanLeakedSource();setTimeout(cleanLeakedSource,250);setTimeout(cleanLeakedSource,1000);})();